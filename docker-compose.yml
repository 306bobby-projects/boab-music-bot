services:
  musicbot:
    build: .
    container_name: discord-music-bot
    restart: always
    env_file:
      - .env
    volumes:
      - .:/app
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
